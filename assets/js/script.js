/**
 * @file
 * Specific behavior theme.
 */
(function ($, Drupal) {

    'use strict';

    /**
     * Add specific behaviors.
     */
    Drupal.behaviors.THEMENAME = {
        attach: function (context, settings) {
            /**/
        }
    };

})(jQuery, Drupal);